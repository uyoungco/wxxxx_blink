<!--components/navi/navi.wxml-->
<view class="container">
  <image bind:tap="onLeft" class="icon" src="{{first ? disLeftSrc : leftSrc}}" />
  <text class="title">{{title}}</text>
  <image bind:tap="onRight" class="icon" src="{{latest ? disRightSrc : rightSrc}}" />
</view>
